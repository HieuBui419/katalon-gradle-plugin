name: Gradle

on:
  push:
    branches: master
   # pull_request:
   # branches: [ $default-branch ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Build Only
        run: | 
          gradle init
          gradle clean build
